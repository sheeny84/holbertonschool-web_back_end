// update Holberton school to add an address
// update is deprecated, so use updateMany
db.school.update(
    { name: "Holberton school" },
    { $set: 
        { address: "972 Mission street"}
    },
    { multi: true }
)
